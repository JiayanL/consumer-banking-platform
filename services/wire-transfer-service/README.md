# wire-transfer-service

Spring Boot 2.7.18 (deliberately older — migration to 3.x tracked in
PLAT-1623) REST service for wire transfer initiation, lookup, and
cancellation. Uses `javax.*` imports. Storage is in-memory H2 via
Spring Data JPA.

Latest JaCoCo instruction coverage: **31.73%**.
