# account-service

Spring Boot 3.3 REST service for consumer account CRUD, balance
inquiry, and freeze/unfreeze lifecycle. Storage is in-memory H2 via
Spring Data JPA.

Run `mvn test` to execute unit tests and generate the JaCoCo report
under `target/site/jacoco/`.

Latest JaCoCo instruction coverage: **33.97%**.
