server:
  port: 8082

spring:
  application:
    name: ledger-service
  datasource:
    url: jdbc:h2:mem:ledgerdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: update

cbp:
  auth:
    secret: "dev-shared-secret-change-me-please-1234567890"
