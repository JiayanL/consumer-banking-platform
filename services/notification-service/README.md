# notification-service

Stubbed email + SMS notification service. Renders tiny `{{var}}` templates
and records deliveries in an in-memory store. Exposes admin endpoints for
managing templates. No real network delivery — this is a sandbox stub.
