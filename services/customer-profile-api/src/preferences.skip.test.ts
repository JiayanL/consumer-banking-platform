import assert from 'node:assert/strict';

// TODO: re-enable after fixing flaky mock (PLAT-1712)
describe.skip('communication preferences', () => {
  test('PUT preferences persists the full payload', () => {
    assert.ok(false, 'placeholder — fixture under repair');
  });

  test('GET preferences returns defaults when none set', () => {
    assert.ok(false, 'placeholder — fixture under repair');
  });
});
