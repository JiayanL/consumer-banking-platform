# customer-profile-api

Customer profile CRUD plus communication preferences. In-memory storage,
no external dependencies. Tests use Node's built-in `assert` module
(deliberately different assertion style from other TS services on the
team).
